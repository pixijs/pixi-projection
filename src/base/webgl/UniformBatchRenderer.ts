namespace pixi_projection {
    import TYPES = PIXI.TYPES;
    import premultiplyTint = PIXI.utils.premultiplyTint;

    export class UniformBatchRenderer extends PIXI.BatchRenderer {
        addToBatch(sprite: PIXI.Sprite) {
        }
    }
}
